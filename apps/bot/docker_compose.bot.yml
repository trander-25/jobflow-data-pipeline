version: "3.9"

services:
  bot:
    build: .
    container_name: bot
    env_file:
      - ../.env
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8001:8001"
    depends_on:
      - api_layer
